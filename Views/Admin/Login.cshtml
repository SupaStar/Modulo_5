@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    Layout = "~/Views/Shared/Admin/_Layout.cshtml";
}
@model UsuarioModel
<a asp-controller="Urgencias" asp-action="Administrador">xdddd</a>
<section>
    @using (Html.BeginForm("IniciarS", "Admin", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        <div class="form-group">
            @Html.LabelFor(x => x.Correo, new { @class = "col-form-label" })
            @Html.TextBoxFor(x => x.Correo,
           new { @class = "form-control", placeholder = "email@example.com", autofocus = "autofocus" })
            @Html.ValidationMessageFor(x => x.Correo, null, new { @class = "text-danger" })
        </div>
        <div class="form-group">
            @Html.LabelFor(x => x.Password, new { @class = "col-form-label" })
            @Html.TextBoxFor(x => x.Password,
           new { @class = "form-control", placeholder = "Password" })
            @Html.ValidationMessageFor(x => x.Password, null, new { @class = "text-danger" })
        </div>
        <div class="form-group">
            <div class="form-check">
                <input type="checkbox" class="form-check-input" id="dropdownCheck2">
                <label class="form-check-label" for="dropdownCheck2">
                    Recuerdame
                </label>
            </div>
        </div>
        <button type="submit" class="btn btn-primary">Iniciar sesion</button>
    }
</section>